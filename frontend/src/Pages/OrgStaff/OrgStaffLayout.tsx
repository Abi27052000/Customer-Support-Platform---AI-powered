// Reverted OrgStaffLayout: placeholder to undo generated layout.
// Keep this file to avoid import errors if referenced elsewhere.
import { Outlet } from 'react-router-dom';

export default function OrgStaffLayout() {
  return (
    <div>
      {/* OrgStaffLayout reverted. Use existing layout components instead. */}
      <Outlet />
    </div>
  );
}
